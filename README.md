## CI/CD Pipeline

This project includes automated CI/CD workflows that run on every push and pull request. The workflow validates code with ESLint, runs tests with Vitest, and builds the project with Vite.